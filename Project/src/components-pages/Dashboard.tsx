// Este componente agora redireciona para Courses
// Mantido para compatibilidade, mas a funcionalidade principal está em Courses.tsx
'use client';

import Courses from './Courses';

export default function Dashboard() {
  return <Courses />;
}
